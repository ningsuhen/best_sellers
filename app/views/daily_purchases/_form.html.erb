<%= simple_form_for(@daily_purchase) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :listing %>
    <%= f.input :day %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
